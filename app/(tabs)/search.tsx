import { View, Text } from 'react-native';

/**
 * Search tab. Placeholder screen; no UI built yet.
 * File: app/(tabs)/search.tsx — route /search.
 */
export default function SearchScreen() {
  return (
    <View>
      <Text>Search</Text>
    </View>
  );
}
